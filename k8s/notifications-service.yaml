apiVersion: v1
kind: Service
metadata:
  name: notifications-service
  namespace: fcg-apps
  labels:
    app: notifications-api
spec:
  type: NodePort
  selector:
    app: notifications-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30083